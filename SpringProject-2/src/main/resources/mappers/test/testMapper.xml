<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
						 "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="kr.co.green.test.model.mapper.TestMapper">

<resultMap id="testResultMap"  type="kr.co.green.test.post.dto.PostDTO">

<result property="testId" column="id"></result>
<result property="testTilte" column="title"></result>
<result property="authorName" column="author"></result>
<result property="createDate" column="created_date"></result>

</resultMap>

<select id="getTotalCount">
	SELECT COUNT(*) FROM POSTS_TEST pt
	
</select>

<select id ="list" resultMap="testResultMap">
      SELECT *
      FROM (
         SELECT id,
               title,
               author,
               created_date,
               ROW_NUMBER() OVER (ORDER BY id) AS rn
         FROM posts_test
      )
      WHERE rn BETWEEN #{pi.offset} AND #{pi.limit}
	

</select>
</mapper>